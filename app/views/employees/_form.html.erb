<%= form_for(@employee, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @employee.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h4>

      <ul>
      <% @employee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :code, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :code, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :fullname, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :fullname, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :emailid, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :emailid, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :contactnum, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :contactnum, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
